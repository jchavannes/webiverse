# The Webiverse

## Dev
```sh
# NPM install
cd ws/files
npm install
cd -

# Start dev (detached)
docker-compose --profile dev up -d

# Stop dev
docker-compose --profile dev down
```

Visit: http://localhost:8233/
